public class EventOnlyInteractable : Interactable
{
    // Does not need code because it will be inheriting from the "Interactable"
    // script which will add interactable functionality without adding additional code
}
